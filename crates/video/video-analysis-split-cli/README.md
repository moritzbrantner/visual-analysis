# video-analysis-split-cli

Thin command-line adapter for `moritzbrantner-video-analysis-split`.

Run:

```bash
cargo run -p video-analysis-split-cli -- operations --json
cargo run -p video-analysis-split-cli -- run --operation describe --json '{"includeOperations":true}'
```
