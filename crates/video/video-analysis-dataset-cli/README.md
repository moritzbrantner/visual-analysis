# video-analysis-dataset-cli

Thin command-line adapter for `moritzbrantner-video-analysis-dataset`.

Run:

```bash
cargo run -p video-analysis-dataset-cli -- operations --json
cargo run -p video-analysis-dataset-cli -- run --operation describe --json '{"includeOperations":true}'
```
