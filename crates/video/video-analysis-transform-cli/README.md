# video-analysis-transform-cli

Thin command-line adapter for `moritzbrantner-video-analysis-transform`.

Run:

```bash
cargo run -p video-analysis-transform-cli -- operations --json
cargo run -p video-analysis-transform-cli -- run --operation describe --json '{"includeOperations":true}'
```
