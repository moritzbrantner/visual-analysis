# video-analysis-core-cli

Thin command-line adapter for `moritzbrantner-video-analysis-core`.

Run:

```bash
cargo run -p video-analysis-core-cli -- operations --json
cargo run -p video-analysis-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
