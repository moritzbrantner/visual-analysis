# video-analysis-ffmpeg-cli

Thin command-line adapter for `moritzbrantner-video-analysis-ffmpeg`.

Run:

```bash
cargo run -p video-analysis-ffmpeg-cli -- operations --json
cargo run -p video-analysis-ffmpeg-cli -- run --operation describe --json '{"includeOperations":true}'
```
