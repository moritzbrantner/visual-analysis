# video-analysis-recognition-cli

Thin command-line adapter for `moritzbrantner-video-analysis-recognition`.

Run:

```bash
cargo run -p video-analysis-recognition-cli -- operations --json
cargo run -p video-analysis-recognition-cli -- run --operation describe --json '{"includeOperations":true}'
```
