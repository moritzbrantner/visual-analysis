# image-analysis-synthesis-cli

Thin command-line adapter for `moritzbrantner-image-analysis-synthesis`.

Run:

```bash
cargo run -p image-analysis-synthesis-cli -- operations --json
cargo run -p image-analysis-synthesis-cli -- run --operation describe --json '{"includeOperations":true}'
```
