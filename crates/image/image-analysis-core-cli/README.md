# image-analysis-core-cli

Thin command-line adapter for `moritzbrantner-image-analysis-core`.

Run:

```bash
cargo run -p image-analysis-core-cli -- operations --json
cargo run -p image-analysis-core-cli -- run --operation describe --json '{"includeOperations":true}'
```
