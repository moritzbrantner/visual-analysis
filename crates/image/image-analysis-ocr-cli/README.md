# image-analysis-ocr-cli

Thin command-line adapter for `moritzbrantner-image-analysis-ocr`.

Run:

```bash
cargo run -p image-analysis-ocr-cli -- operations --json
cargo run -p image-analysis-ocr-cli -- run --operation describe --json '{"includeOperations":true}'
```
