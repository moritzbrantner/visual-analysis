# image-analysis-processing-cli

Thin command-line adapter for `moritzbrantner-image-analysis-processing`.

Run:

```bash
cargo run -p image-analysis-processing-cli -- operations --json
cargo run -p image-analysis-processing-cli -- run --operation describe --json '{"includeOperations":true}'
```
