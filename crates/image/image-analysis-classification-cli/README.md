# image-analysis-classification-cli

Thin command-line adapter for `moritzbrantner-image-analysis-classification`.

Run:

```bash
cargo run -p image-analysis-classification-cli -- operations --json
cargo run -p image-analysis-classification-cli -- run --operation describe --json '{"includeOperations":true}'
```
