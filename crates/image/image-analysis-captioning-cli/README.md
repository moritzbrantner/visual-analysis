# image-analysis-captioning-cli

Thin command-line adapter for `moritzbrantner-image-analysis-captioning`.

Run:

```bash
cargo run -p image-analysis-captioning-cli -- operations --json
cargo run -p image-analysis-captioning-cli -- run --operation describe --json '{"includeOperations":true}'
```
