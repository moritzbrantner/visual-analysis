# image-analysis-segmentation-cli

Thin command-line adapter for `moritzbrantner-image-analysis-segmentation`.

Run:

```bash
cargo run -p image-analysis-segmentation-cli -- operations --json
cargo run -p image-analysis-segmentation-cli -- run --operation describe --json '{"includeOperations":true}'
```
