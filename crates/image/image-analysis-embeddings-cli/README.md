# image-analysis-embeddings-cli

Thin command-line adapter for `moritzbrantner-image-analysis-embeddings`.

Run:

```bash
cargo run -p image-analysis-embeddings-cli -- operations --json
cargo run -p image-analysis-embeddings-cli -- run --operation describe --json '{"includeOperations":true}'
```
