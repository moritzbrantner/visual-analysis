# Triage labels

- `ready-for-agent`: agent-ready work.
- `agent-loop:active`: one worker owns the slice.
- `agent-loop:blocked`: an external prerequisite prevents progress.
- `agent-loop:ready-to-merge`: exact-head evidence and review are complete.
