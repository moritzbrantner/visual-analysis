schema_version = 1

[policy]
track = "latest-stable"

[system]
apt = []

[setup]
commands = ["bun install --frozen-lockfile", "cargo fetch"]

[maintenance]
commands = ["bun install --frozen-lockfile", "cargo fetch"]

[cache]
paths = ["~/.cargo", "~/.bun/install/cache"]

[compatibility_holds]
