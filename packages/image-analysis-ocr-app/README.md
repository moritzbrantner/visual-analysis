# image-analysis-ocr-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `image-analysis-ocr`.

Run the server:

```bash
cargo run -p image-analysis-ocr-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/image-analysis-ocr-app dev
```
