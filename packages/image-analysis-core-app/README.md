# image-analysis-core-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `image-analysis-core`.

Run the server:

```bash
cargo run -p image-analysis-core-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/image-analysis-core-app dev
```
