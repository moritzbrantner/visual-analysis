# video-analysis-ffmpeg-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-ffmpeg`.

Run the server:

```bash
cargo run -p video-analysis-ffmpeg-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-ffmpeg-app dev
```
