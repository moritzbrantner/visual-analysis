# video-analysis-output-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-output`.

Run the server:

```bash
cargo run -p video-analysis-output-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-output-app dev
```
