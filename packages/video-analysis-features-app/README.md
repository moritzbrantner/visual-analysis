# video-analysis-features-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-features`.

Run the server:

```bash
cargo run -p video-analysis-features-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-features-app dev
```
