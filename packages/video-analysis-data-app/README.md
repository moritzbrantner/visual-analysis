# video-analysis-data-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-data`.

Run the server:

```bash
cargo run -p video-analysis-data-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-data-app dev
```
