# video-analysis-editing-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-editing`.

Run the server:

```bash
cargo run -p video-analysis-editing-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-editing-app dev
```
