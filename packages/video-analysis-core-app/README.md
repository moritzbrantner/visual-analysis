# video-analysis-core-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-core`.

Run the server:

```bash
cargo run -p video-analysis-core-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-core-app dev
```
