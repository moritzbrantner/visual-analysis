# video-analysis-storage-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-storage`.

Run the server:

```bash
cargo run -p video-analysis-storage-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-storage-app dev
```
