# video-analysis-ingest-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `video-analysis-ingest`.

Run the server:

```bash
cargo run -p video-analysis-ingest-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/video-analysis-ingest-app dev
```
