# image-analysis-io-app

React, TypeScript, TailwindCSS, Bun, and oxfmt frontend for `image-analysis-io`.

Run the server:

```bash
cargo run -p image-analysis-io-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun run --cwd packages/image-analysis-io-app dev
```
